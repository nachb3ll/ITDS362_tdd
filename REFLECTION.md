1. มี​เทสต์​ทั้งหมด​กี่​ตัว​และ​โค้ด​จริง​กี่​บรรทัด​อัตราส่วน​เป็น​เท่าใด
Ans มีเทสต์ทั้งหมด 4 ตัว และโค้ดจริงใน `kitchen.py` มีประมาณ 35 บรรทัด อัตราส่วนระหว่างบรรทัดของเทสต์กับโค้ดจริงอยู่ที่ประมาณ 1:1.5
2. ช่วง​ที่​แถบ​เป็น​สีแดง​นาน​ที่สุด​คือ​เมื่อใด​อะไร​ทำให้​มัน​นาน
Ans ช่วงขั้นตอน A6 (การบวกปริมาณสองค่า) นานที่สุด เพราะต้องมีการคิดเชิงระบบเพิ่มเติม ต้องสร้างทั้งคลาส `Sum` และคลาส `Converter` ขึ้นมาทำงานร่วมกับ `Quantity` ซึ่งซับซ้อนกว่าการแก้โค้ดแค่เมท็อดเดียว
3. หา​จุด​ที่​ใช้ Fake It หนึ่ง​จุด​ใช้ Triangulate หนึ่ง​จุด​และ​ใช้ Obvious Implementation หนึ่ง​จุด​เมื่อ​มอง​ย้อนกลับ​ไป​แต่ละครั้ง​เลือก​ได้​เหมาะสม​หรือไม่
Ans 
- Fake It = ขั้นตอน A1 มีการคืนค่า `600` เพื่อให้ระบบ test ทำงานได้ก่อน
- Triangulate = ขั้นตอน A2 ในการเขียน test `times(2) == 400` เพิ่มเติมเพื่อบังคับให้ต้องเขียนสูตรคูณจริง ๆ (`amount * multiplier`)
- Obvious Implementation = ขั้นตอน A4 ใน method `__eq__` เพื่อเช็คความเท่ากัน ซึ่งมีวิธีเขียนที่ชัดเจนอยู่แล้ว ไม่จำเป็นต้อง Fake
- เมื่อ​มอง​ย้อนกลับ​ไปใน​แต่ละครั้ง​เลือกสามารถเลือก​ได้​เหมาะสม
4. ยังมี​รายการ​ใด​ค้าง​อยู่​ใน​รายการ​ทดสอบ​รายการ​ใด​ที่จะ​ลงมือทำ​จริง​และ​รายการ​ใด​ที่​ควร​ลบทิ้ง​เพราะ​เป็นการ​คาดเดา​ล่วงหน้า
Ans
- รายการที่ค้างอยู่ คือ "200 g + 1 oz แปลงผลลัพธ์เป็นกรัมโดยใช้อัตราแปลงหน่วย" และ "(200 g + 1 oz) x 2"
- รายการที่ควรทำจริง คือ การบวกข้ามหน่วย (g + oz) เพราะมีการเตรียมโครงสร้าง `Converter` ไว้แล้ว
- รายการที่ควรลบทิ้ง คือ การบวกแบบไม่มีหน่วย เช่น 200 + 300 = 500 เพราะตอนนี้ระบบรองรับหน่วยได้สมบูรณ์แล้ว
5. ดู​เมท็อด Quantity.reduce ถ้า​ออกแบบ​ระบบ​ทั้งหมด​บน​กระดาษ​ก่อนนักศึกษาจะ​เขียน​เมท็อด​นี้​ออกมา​หรือไม่
Ans ไม่เขียนออกมา เพราะถ้าคิดล่วงหน้าแบบ Big Design Up Front จะรวมทุกอย่างไว้ใน `Converter` หรือ `plus` จนกลายเป็นคลาสที่ใหญ่ แต่การใช้ TDD จะทำให้เห็นปัญหา และเห็นความจำเป็นของการใช้ Polymorphism ให้แต่ละ Object จัดการ Reduce ตนเองได้